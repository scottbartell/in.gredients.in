defaults: &defaults
  adapter: postgresql
  encoding: unicode
  pool: 5
  host: localhost
  port: 5432
  username: postgres
  password: postgres

development:
  <<: *defaults
  database: gredients_development

production:
  <<: *defaults
  database: gredients_production

test:
  <<: *defaults
  database: gredients_test